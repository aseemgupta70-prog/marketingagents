# ─────────────────────────────────────────────────────────────────────────────
# GTM Intelligence — Python Dependencies
# Install: pip install -r requirements.txt
# Python: 3.11+
# ─────────────────────────────────────────────────────────────────────────────

# ── AI / Content Generation ───────────────────────────────────────────────────
anthropic>=0.40.0              # Claude claude-opus-4-6 API — required for content generation

# ── Web API ───────────────────────────────────────────────────────────────────
fastapi>=0.115.0               # REST API server
uvicorn[standard]>=0.32.0      # ASGI server for FastAPI

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0              # ORM — async-first
aiosqlite>=0.20.0              # Async SQLite driver
alembic>=1.14.0                # DB migrations

# ── Data Validation ───────────────────────────────────────────────────────────
pydantic>=2.0.0                # Data models + validation
pydantic-settings>=2.0.0       # Settings from .env

# ── Ingestion: Reddit ─────────────────────────────────────────────────────────
praw>=7.8.0                    # Reddit API client (PRAW)

# ── Ingestion: HTTP / RSS ─────────────────────────────────────────────────────
feedparser>=6.0.11             # RSS/Atom feed parser
httpx>=0.27.0                  # Async HTTP client (HackerNews Algolia API)
aiohttp>=3.10.0                # Async HTTP sessions

# ── NLP / Text Analysis ───────────────────────────────────────────────────────
scikit-learn>=1.5.0            # TF-IDF keyword extraction
vaderSentiment>=3.3.2          # Sentiment analysis → pain intensity scoring
numpy>=1.26.0                  # Numerical operations
nltk>=3.9.0                    # Tokenization, stopwords
spacy>=3.8.0                   # NLP pipeline (optional — used for NER)

# ── Scheduling ────────────────────────────────────────────────────────────────
apscheduler>=3.10.4            # In-process cron scheduler

# ── CLI & Terminal UI ─────────────────────────────────────────────────────────
click>=8.1.7                   # CLI framework
rich>=13.9.0                   # Rich terminal output (tables, panels, progress)

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0           # Load .env into os.environ
pyyaml>=6.0.2                  # Parse config/settings.yaml and config/sources.yaml
tenacity>=9.0.0                # Retry logic for API calls
python-dateutil>=2.9.0         # Date parsing utilities

# ── Dev / Testing ─────────────────────────────────────────────────────────────
pytest>=8.3.0                  # Test runner
pytest-asyncio>=0.24.0         # Async test support (in-memory SQLite)
pytest-httpx>=0.30.0           # Mock httpx calls in tests
